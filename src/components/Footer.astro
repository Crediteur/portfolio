---
import { links } from "../data/constants";
---

<footer>
  <div
    id="contact"
    class="mx-auto max-w-screen-lg p-8">
    <ul class="flex flex-row justify-center items-center gap-10 py-8">
      {
        links.map((link) => (
          <li>
            <a
              href={link.url}
              target="_blank"
              rel="noopener noreferrer">
              <img
                src={link.icon}
                alt={`${link.name} icon`}
                class="social-icon w-10 h-10"
              />
            </a>
          </li>
        ))
      }
    </ul>
    <div class="flex flex-col justify-end text-right">
      <p class="text-xs translate-y-4">
        © {new Date().getFullYear()} Tony Yang
      </p>
      <p class="text-[0.5rem] translate-y-5">
        <a
          class="hover:underline"
          href="https://astro.build/"
          target="_blank"
          rel="noopener noreferrer">
          made with Astro 🚀
        </a>
      </p>
    </div>
  </div>
</footer>

<style>
  .social-icon {
    filter: invert(0.7);
    transition: filter 0.2s ease-in-out;
  }
  .social-icon:hover {
    filter: invert(0.9);
  }
</style>
